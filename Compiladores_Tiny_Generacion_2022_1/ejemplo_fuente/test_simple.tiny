{Programa simple para probar arrays}

global arr : array[5];

begin
    var i;
    
    // Inicializar array
    for i := 0 to 4 step 1
        arr[i] := i + 1
    end;
    
    // Mostrar valores
    for i := 0 to 4 step 1
        write arr[i]
    end
end